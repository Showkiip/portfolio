import React from 'react'
import { Box, Button, Stack, Typography, Modal, TextField } from '@mui/material';

const Navbar = () => {
  return (
    <Stack sx={{ height: '2.8rem'}}>
      <Box>
        <Typography variant="h6">
          Navbar
        </Typography>
      </Box>
    </Stack>
  )
}

export default Navbar